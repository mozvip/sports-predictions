angular.module('sports-predictions')
        .factory('StatsService', ['$rootScope', '$http', '$q', '$linq', 'UserService', function ($rootScope, $http, $q, $linq, UserService) {

                return {
                        getStatPredictionUser: function () {
                                // TODO : Graph
                        }
				}
}]);