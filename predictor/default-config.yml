#secrets
googleReCaptchaSecretKey: XXXXXXXXXXXXXXXXXXXXXXXXXXX
footballDataApiKey: XXXXXXXXXXXXXXXXXXXX

publicDomain: wc2018.soccer
defaultCommunity: grand-est
eventName: World Cup 2018

# set to true to enable the PhaseFilter that redirects use to coming-soon among other things
liveWebSite: true

googleReCaptchaEnabled : false

server:
  type: simple
  applicationContextPath: /
  adminContextPath: /admin
  rootPath: /api/*
  connector:
    type: http
    port: 9000

database:
  # the name of your JDBC driver
  driverClass: org.h2.Driver

  # the username
  user: admin

  # the password
  password: admin

  # the JDBC URL
  url: jdbc:h2:./sports-predictions

  # any properties specific to your JDBC driver:
  properties:
    charSet: UTF-8

  # the maximum amount of time to wait on an empty pool before throwing an exception
  maxWaitForConnection: 1s

  # the SQL query to run when validating a connection's liveness
  validationQuery: "/* MyService Health Check */ SELECT 1"

  # the minimum number of connections to keep open
  minSize: 8

  # the maximum number of connections to keep open
  maxSize: 32

  # whether or not idle connections should be validated
  checkConnectionWhileIdle: false
